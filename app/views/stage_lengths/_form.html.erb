<%= form_for(@stage_length) do |f| %>
  <% if @stage_length.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@stage_length.errors.count, "error") %> prohibited this stage_length from being saved:</h2>

      <ul>
      <% @stage_length.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
