<h1>Listing stage_lengths</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @stage_lengths.each do |stage_length| %>
  <tr>
    <td><%= link_to 'Show', stage_length %></td>
    <td><%= link_to 'Edit', edit_stage_length_path(stage_length) %></td>
    <td><%= link_to 'Destroy', stage_length, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Stage length', new_stage_length_path %>
