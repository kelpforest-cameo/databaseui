<%= form_for(@stage_duration) do |f| %>
  <% if @stage_duration.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@stage_duration.errors.count, "error") %> prohibited this stage_duration from being saved:</h2>

      <ul>
      <% @stage_duration.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
