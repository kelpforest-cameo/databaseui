<%= form_for(@competition_interaction) do |f| %>
  <% if @competition_interaction.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@competition_interaction.errors.count, "error") %> prohibited this competition_interaction from being saved:</h2>

      <ul>
      <% @competition_interaction.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
