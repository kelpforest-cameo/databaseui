<%= form_for(@node_range) do |f| %>
  <% if @node_range.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@node_range.errors.count, "error") %> prohibited this node_range from being saved:</h2>

      <ul>
      <% @node_range.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
