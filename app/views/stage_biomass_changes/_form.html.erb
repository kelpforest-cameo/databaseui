<%= form_for(@stage_biomass_change) do |f| %>
  <% if @stage_biomass_change.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@stage_biomass_change.errors.count, "error") %> prohibited this stage_biomass_change from being saved:</h2>

      <ul>
      <% @stage_biomass_change.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
