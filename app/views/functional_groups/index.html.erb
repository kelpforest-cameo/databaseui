<h1>Listing functional_groups</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @functional_groups.each do |functional_group| %>
  <tr>
    <td><%= link_to 'Show', functional_group %></td>
    <td><%= link_to 'Edit', edit_functional_group_path(functional_group) %></td>
    <td><%= link_to 'Destroy', functional_group, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Functional group', new_functional_group_path %>
