<%= form_for(@functional_group) do |f| %>
  <% if @functional_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@functional_group.errors.count, "error") %> prohibited this functional_group from being saved:</h2>

      <ul>
      <% @functional_group.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
