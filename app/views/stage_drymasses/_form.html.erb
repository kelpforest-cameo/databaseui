<%= form_for(@stage_drymass) do |f| %>
  <% if @stage_drymass.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@stage_drymass.errors.count, "error") %> prohibited this stage_drymass from being saved:</h2>

      <ul>
      <% @stage_drymass.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
