<%= form_for(@stage_fecundity) do |f| %>
  <% if @stage_fecundity.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@stage_fecundity.errors.count, "error") %> prohibited this stage_fecundity from being saved:</h2>

      <ul>
      <% @stage_fecundity.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
