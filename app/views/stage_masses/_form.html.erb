<%= form_for(@stage_mass) do |f| %>
  <% if @stage_mass.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@stage_mass.errors.count, "error") %> prohibited this stage_mass from being saved:</h2>

      <ul>
      <% @stage_mass.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
